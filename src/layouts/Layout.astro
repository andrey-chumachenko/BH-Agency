---
import '../styles/tokens.css';
import '../styles/base.css';
import '../styles/utilities.css';
import '../styles/components.css';
import themeScript from '../scripts/theme.ts?url';
import mobileMenuScript from '../scripts/mobile-menu.ts?url';
import lightboxScript from '../scripts/lightbox.ts?url';
import calculatorScript from '../scripts/calculator.ts?url';
import headerScrollScript from '../scripts/header-scroll.ts?url';
import scrollRevealScript from '../scripts/scroll-reveal.ts?url';
import teamDragScript from '../scripts/team-drag.ts?url';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';

interface Props {
  title?: string;
  description?: string;
}

const {
  title = 'BH Agency — YouTube + AI-відео для бізнесу',
  description = 'BH Agency створює YouTube-системи та AI-відео для бізнесу: стратегія, продакшн, зростання і результати під ключ.',
} = Astro.props;

const canonical = new URL(Astro.url.pathname, 'https://www.bhagency.com.ua').toString();
const organizationSchema = {
  '@context': 'https://schema.org',
  '@type': 'Organization',
  name: 'BH Agency',
  url: 'https://www.bhagency.com.ua',
  email: 'bhagency.ai@gmail.com',
};
---

<!doctype html>
<html lang="uk">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
    <meta name="description" content={description} />
    <meta property="og:type" content="website" />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={canonical} />
    <meta property="og:image" content="/og-image.png" />
    <meta property="og:site_name" content="BH Agency" />
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content="/og-image.png" />
    <link rel="canonical" href={canonical} />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32.png" />
    <link rel="apple-touch-icon" href="/apple-touch-icon.png" />
    <script type="application/ld+json" set:html={JSON.stringify(organizationSchema)} />
    <script is:inline>
      (() => {
        const stored = localStorage.getItem('theme');
        const preferred = window.matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light';
        document.documentElement.dataset.theme = stored || preferred;
      })();
    </script>
  </head>
  <body>
    <a class="skip-link button button-primary" href="#main">До контенту</a>
    <Header />
    <main id="main">
      <slot />
    </main>
    <Footer />
    <script src={themeScript} defer></script>
    <script src={mobileMenuScript} defer></script>
    <script src={lightboxScript} defer></script>
    <script src={calculatorScript} defer></script>
    <script src={headerScrollScript} defer></script>
    <script src={scrollRevealScript} defer></script>
    <script src={teamDragScript} defer></script>
  </body>
</html>
